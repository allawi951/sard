# خطة تسليم ثيم «سرد» — من نموذج عامل إلى ثيم جاهز للنشر

**المشروع:** `C:\Projects\sard` · مستودع `allawi951/sard` فرع `master`
**الحالة:** الهوية كاملة في كل الصفحات · **كل قياسات Lighthouse تتجاوز الحدّ**
(الرئيسية على سطح المكتب على الحافّة ٥٩–٦١) · المتبقّي خطوات المالك في اللوحة.
**آخر تحديث:** 2026-08-01

---

## 📌 الافتراضات المعتمدة (مُتحقَّق منها من توثيق سلة)

| # | الحقيقة | المصدر |
|---|---|---|
| أ‑1 | **العناصر لا تُضاف تلقائيًا** لصفحة التاجر. الترتيب الافتراضي يضبطه **مطوّر الثيم من لوحة الشركاء**، ثم يرثه كل متجر يطبّق الثيم. | [توثيق الصفحة الرئيسية](https://docs.salla.dev/422558m0) |
| أ‑2 | `is_default` في `twilight.json` **ليس موثّقًا** كآلية زرع تلقائي — الاعتماد عليه وحده خطأ. | لا ذكر له في توثيق `twilight.json` |
| أ‑3 | **حجم الثيم ≤ 1MB** شرط قبول. | [المراجعة التقنية](https://docs.salla.dev/421888m0) بند 1.1 |
| أ‑4 | **Lighthouse:** أداء ≥ 60 · وصولية ≥ 90 — على الرئيسية وصفحة المنتج وقائمة المنتجات، جوال وسطح مكتب. | المراجعة التقنية 2.1.1 / 2.1.2 |
| أ‑5 | **ممنوع** `\|raw` وممنوع تمكين التاجر من حقن HTML. | المراجعة التقنية بند 5 |
| أ‑6 | **ممنوع النصوص الثابتة** — كل نص للمستخدم عبر الترجمة. | المراجعة التقنية 1.2 |
| أ‑7 | **`trans` دالةٌ لا فلتر.** الفلتر المجهول خطأ وقت ترجمة يُصيِّر المكوّن فراغًا بلا رسالة. | استُخرج من مفردات ثيم سلة الرسمي (١٧ فلترًا، ليس فيها `trans`) |

---

## 🎯 الهدف

ثيم «سرد» **كامل الهوية في كل صفحة**، يجتاز مراجعة سلة، وجاهز للنشر.

---

## 📊 القياس الحالي مقابل المطلوب

| المعيار | الآن (مقيس) | المطلوب | الحالة |
|---|---|---|---|
| هوية سرد — ١٥ صفحة × ٣ مقاسات × اتجاهين | **٠ إخفاق** (`theme-audit`) | كل الصفحات | 🟢 |
| هوية **الكتل** (منتج · فلاتر · تقييمات · «قد يعجبك» …) | **٠ إخفاق** (`component-audit`) | كل الكتل | 🟢 |
| Lighthouse وصولية | **١٠٠** رئيسية · **٩٥** منتج · **٩٨** قائمة | ≥ 90 | 🟢 |
| Lighthouse أداء — صفحة المنتج | **٩٤** جوال · **٧٣** مكتب | ≥ 60 | 🟢 |
| Lighthouse أداء — قائمة المنتجات | **٩٤** جوال · **٨٥** مكتب | ≥ 60 | 🟢 |
| Lighthouse أداء — **الرئيسية** | **٨٢–٨٧** جوال · **٥٩–٦١** مكتب | ≥ 60 | 🟡 على الحافّة |
| حجم المستودع المتتبَّع | **3.0 MB** (كان 6.7) · `src` 1.1MB | ≤ 1 MB | 🟡 |
| `\|raw` في قوالب سرد | صفر | صفر | 🟢 |
| نصوص ثابتة | صفر (`i18n-audit`) | صفر | 🟢 |
| فلاتر خارج مفردات سلة | صفر (`validate`) | صفر | 🟢 |
| الترتيب الافتراضي في اللوحة | غير مضبوط | مضبوط | 🔵 بيد المالك |

> **عن قياس الأداء:** يجري محليًّا على `.preview` بخادم يضغط النصوص (بدون
> الضغط يخرج LCP وهميًّا ٨–١٠ ثوانٍ لا يقع على المنصّة). النتيجة **مؤشّر لا
> شهادة**؛ القياس الحاسم على متجر المعاينة. تذبذب الرئيسية ±١٠ نقاط بين
> الجولات مقيسٌ أيضًا، فتُقرأ كمدى لا كرقم.

---

## 🗂️ الخطوات

### المرحلة أ — إزالة الحواجز

| # | الخطوة | المخرج | الحالة |
|---|---|---|---|
| 1 | **تنحيف الثيم** — إخراج `preview/` و`shots/`، تقليم الخطوط، تنظيف التجهيزة، ضبط `.gitignore` | المتتبَّع 3.0MB · `src` 1.1MB | 🟡 12MB → 3.0MB (الحدّ 1MB) |
| 2 | **ضبط الترتيب الافتراضي في لوحة الشركاء** | كل متجر جديد يرث ترتيب سرد | 🔵 بيد المالك |
| 3 | **مكوّنات الواجهة** — سرد + كل مكوّنات الهيكل | **١١ مكوّنًا · ١٧ ميزة** | ✅ أُعيد ما قُلِّم خطأً |

### المرحلة ب — هوية سرد في كل صفحة

| # | الخطوة | المخرج | الحالة |
|---|---|---|---|
| 4 | الشريط العلوي والذيل بهوية سرد | `_sard-chrome.scss` | ✅ |
| 5 | **صفحة المنتج** | خطّ سرد · أزرار دائرية · تباين مصحّح | ✅ مقيس |
| 6 | **قائمة المنتجات والتصنيفات** | هوية + شبكة | ✅ مقيس |
| 7 | **السلة وشكرًا لك** | هوية كاملة | ✅ مقيس |
| 8 | **صفحات الحساب** (الملف · الطلبات · الأمنيات · الإشعارات) | هوية كاملة | ✅ مقيس |
| 9 | **التعريفية والمدونة والماركات** | هوية كاملة | ✅ مقيس |
| 10 | جلد عام موحّد | `_sard-global.scss` | ✅ |

> الخطوات ٥–٩ لم تُقَس بالعين بل بأداة: `tools/theme-audit.mjs` تفحص كل صفحة
> على خطّ العناوين والمتن، ورموز سرد، والشريط والذيل، وحوافّ الأزرار،
> وأخطاء الجافاسكربت — ثم الاستجابة والاتجاه بثلاثة مقاسات.

### المرحلة ج — الجودة والامتثال

| # | الخطوة | المخرج | الحالة |
|---|---|---|---|
| 11 | الترجمة — صفر نص ثابت | `i18n-audit` | ✅ |
| 12 | **Lighthouse** | وصولية 100/95/98 · أداء 87/94/94 جوال · 61/73/85 مكتب | ✅ |
| 13 | **فحص الاستجابة على الثيم كاملًا** | ١٥ صفحة × ٣ مقاسات = ٠ إخفاق | ✅ |
| 14 | **RTL/LTR لكل صفحة** | الاتجاهان = ٠ إخفاق | ✅ |

### المرحلة د — التجهيز للنشر

| # | الخطوة | المخرج | الحالة |
|---|---|---|---|
| 15 | مميزات الثيم ووصفه | `THEME_LISTING.md` | ✅ |
| 16 | صور الثيم | `sard-marketing/preview/` | ✅ |
| 16‑أ | **هوية سرد** (رمز · كلمة · قفل · أيقونات · غلاف) | `brand/assets` | ✅ |
| 17 | متجر معاينة + سعر + معلومات دعم | خيارات النشر | 🔵 بيد المالك |
| 18 | مراجعة نهائية ثم `salla theme publish` | مقدَّم للمراجعة | ⬜ بعد ١٢ و١٧ |

---

## ✅ ما أُغلق: أداء الرئيسية — وسببه لم يكن ما ظننت

كان المقيس: جوال ٥٥–٦٤ · مكتب ٣٧–٤٢. وبعد الإصلاح: **٨٢–٨٧ / ٥٩–٦١**.

السبب لم يكن CSS ولا الخطوط ولا حركة الواجهة — بل **علّة في أداة المعاينة
نفسها**: `is_page('index')` كانت تقارن بصفحة التجهيزة الثابتة لا بالصفحة
الجاري تصييرها، فيُطبَّق صنف `sard-home` على **كل** صفحة. أثره:

  · الجلد الداكن للواجهة يُطبَّق على صفحة المنتج والسلة والحساب — أي أن
    كل تدقيقاتي السابقة كانت تقيس صفحاتٍ على خلفية ليست خلفيتها.
  · حركات الواجهة وقماش الماء (canvas) تعمل في صفحات لا واجهة فيها،
    فتلتهم المعالج وتهبط بالأداء.

الدرس: **قبل الوثوق بأي قياس، تحقّق من أن المحاكي يعكس المنصّة.** ثلاث علل
في المحاكي (مسارات الأصول · كائن salla · is_page) كانت كلها تُكذّب النتائج
في اتجاهين متعاكسين: تُظهر أعطالًا غير موجودة وتُخفي أخرى موجودة.

**ما تبقّى:** الرئيسية على سطح المكتب على الحافّة (٥٩–٦١ والحدّ ٦٠). القياس
الحاسم على متجر المعاينة، حيث الشبكة والتخزين المؤقّت أقرب للواقع.

---

## ⚠️ حدّ المعاينة المحلّية

مكوّنات `<salla-*>` **لا تُصيَّر محليًّا** — سكربتاتها من المنصّة. فما يظهر
في `component-audit` بـ«غير ظاهر في التجهيزة» (الفلاتر، شبكة المنتجات،
قائمة التقييمات، «قد يعجبك») **يُتحقَّق منه على متجر المعاينة لا هنا**.

ما فُعِل لأجلها هنا شيئان قابلان للتحقّق:
  ١. **ربط رموز الهيكل بلوحة سرد** في `master.twig` — وهي الرافعة الكبرى:
     مكوّنات سلة تقرأ `--color-primary` و`--font-main` وأخواتهما، فتتبع
     لوحة سرد الآن بدل لون التاجر. (`component-audit` يتحقّق من الربط.)
  ٢. `_sard-blocks.scss` — أنماط لأغلفة الكتل في الضوء ومقابض `::part()`
     المتاحة (button · card · title · payment).

---

## 🧹 ما أُخرِج من المستودع (2026-08-01)

| البند | كان | لماذا |
|---|---|---|
| `shots/` | 3.2 MB · ١٥ لقطة | مخرجات `responsive-audit` القابلة للتوليد؛ اللقطات التسويقية الحقيقية في `sard-marketing/preview/screens` |
| `tools/fixtures.json` | 313 KB → **8 KB** | تسرّب شعار base64 من اختبار الرسم ولم يُستعد — التجهيزة بيانات لا صور |
| `brand/concepts/sheet.{png,html}` | 296 KB | مخرجات `brand/concepts/sheet.mjs` |
| `.preview/` | 6.8 MB | مخرجات المعاينة المحلية |

**الباقي الأكبر متتبَّعًا:** `public/app.css` 760KB · `public/home.js` 180KB ·
`pnpm-lock.yaml` 216KB. و`public/` **يرفعه هيكل سلة نفسه** (٢٠ ملفًا في
اللقطة الأولى) فلا يُستبعَد.

---

## 🧰 الأدوات

| الأداة | ماذا تفعل |
|---|---|
| `tools/preview.mjs` | يُصيّر **١٥ صفحة** محليًّا (`SARD_DIR=ltr` للاتجاه الآخر) |
| `tools/theme-audit.mjs` | هوية + استجابة + اتجاه لكل صفحة × ٣ مقاسات |
| `tools/lighthouse.mjs` | أداء ووصولية على ٣ صفحات × مقاسين (خادم يضغط) |
| `tools/validate.mjs` | JSON · صفحات إلزامية · توازن Twig · **مفردات الفلاتر** · مفردات `twilight.json` |
| `tools/i18n-audit.mjs` | صفر نص ثابت |
| `brand/build.mjs` | يولّد كل أصول الهوية من مصدر واحد |

---

## 🗂️ سجل القرارات

| التاريخ | القرار | السبب |
|---|---|---|
| 2026-07-31 | بناء سرد فوق هيكل سلة لا من الصفر | المبني من الصفر يُرفض بمُحقّق سلة برسالة عامة |
| 2026-07-31 | صور العرض تخرج من الثيم | حدّ 1MB |
| 2026-07-31 | لا نعتمد `is_default` وحده | غير موثّق؛ الترتيب يُضبط من اللوحة |
| 2026-08-01 | **التلوين هو الافتراضي لشعار التاجر** | شعارٌ داكن على واجهة داكنة يختفي؛ والتاجر يطفئه بمفتاح |
| 2026-08-01 | **كلمة «سرد» لا تظهر في متاجر التجّار** | اسم الثيم علامةُ البائع لا علامةُ التاجر — الرمز وحده يظهر ويُطفأ |
| 2026-08-01 | **قياس الأداء بخادم يضغط** | بلا ضغط يخرج LCP وهميًّا ٨–١٠ث لا يقع على المنصّة |
| 2026-08-01 | **الصفحات الداخلية داكنة كالواجهة** | الجلد الفاتح جعلها تبدو ثيمًا آخر بجانب الواجهة الكحلية؛ التباين كريمي/كحلي ≈ 15:1 |
| 2026-08-01 | **إعادة مكوّنات الهيكل الستّة وميزاته الثلاث** | تقليمها ترك التاجر بخمسة مكوّنات فقط بلا لافتات ولا آراء عملاء ولا ماركات — والقيمة في الجمع لا في الحذف |

## ⚠️ المحاولات الفاشلة (لا تُعاد)

- `format: "color"` في الإعدادات → غير مدعوم، رفض صامت
- `matches` في Twig للكشف عن العربية → كسر التصيير في Twig.js
- تقسيم الاسم العربي إلى `<span>` لكل حرف → يكسر وصل الحروف ويعكسها
- `gsap.from` + ScrollTrigger بلا `immediateRender:false` → يخفي المحتوى للأبد
- **`'key'|trans` كفلتر** → غير موجود في المحرّك؛ يُفشِل القالب كلّه فيُصيَّر فراغًا
- **تتبّع الحدود بمشية مور** → لا تُغلَق على الأشكال الرفيعة؛ البديل مربّعات مسيرة
- **ملء كفاف الشعار** (`evenodd` و `nonzero`) → كتلة صمّاء تطمس التفاصيل
- **قلب ألوان الشعار** (`invert`) → يحلّ الاختفاء ويصنع قفزةً لونية
- **تسريع حركة الواجهة بلا قياس** → أنزل الأداء بدل أن يرفعه
- **تقليم `features` و`components`** ظنًّا أنه «صدق في المزايا» → حرم التاجر من
  مكوّنات المنصّة كلها (لافتات، آراء عملاء، ماركات، روابط سريعة). المزايا
  تُعلَن لأن قوالبها موجودة، لا لأن سرد صمّمها.
- **جلد فاتح للصفحات الداخلية** → بدت ثيمًا آخر بجانب الواجهة الكحلية
